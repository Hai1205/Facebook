# Configuration for spring-dotenv
# Set to false so application can start without .env file (e.g. in Docker with env vars passed directly)
dotenv.required=false

# Path to .env file (relative to project root or absolute)
dotenv.path=.env